pydantic>=2.6
pydantic-settings>=2.2
SQLAlchemy>=2.0
psycopg2-binary>=2.9
feedparser>=6.0
requests>=2.31
beautifulsoup4>=4.12
trafilatura>=1.8
youtube-transcript-api>=0.6
APScheduler>=3.10
python-dateutil>=2.9
# zoneinfo has no timezone database to read on Windows, and market-hours gating,
# log timestamps and alert headers all depend on one. Linux images carry a system
# tzdata, so this only matters for laptop runs -- but it has been arriving as a
# transitive dependency, which is not something to rely on.
tzdata>=2024.1
tenacity>=8.2
anthropic>=0.40
# pytest lives in requirements-dev.txt: nothing at runtime imports it, and it
# dragged ~15 MB into a production image that has to cross a flaky upload path.
